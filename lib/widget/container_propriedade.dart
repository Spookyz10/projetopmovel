import 'package:flutter/material.dart';

class ContainerPropriedades extends StatefulWidget {
  final dynamic propriedade;

  const ContainerPropriedades({super.key, required this.propriedade});

  @override
  State<ContainerPropriedades> createState() => _ContainerPropriedadesState();
}

class _ContainerPropriedadesState extends State<ContainerPropriedades> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
